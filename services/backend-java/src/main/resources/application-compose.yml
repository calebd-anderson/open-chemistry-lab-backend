spring:
  mongodb:
    uri: ${SPRING_MONGODB_URI:mongodb://mongodb:27017}
    database: chem-lab-dev
    # Enable auto-indexing during development
    auto-index-creation: true

services:
  fastapi-worker: ${SERVICES_FASTAPI_WORKER:http://python-worker:80}